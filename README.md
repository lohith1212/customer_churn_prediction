# customer_churn_prediction
Customer Churn Prediction project using an end-to-end ML pipeline with preprocessing, SMOTE balancing, model training, hyperparameter tuning, and a production-ready prediction system. Automatically selects and saves the best model for deployment.
